# Rule-book Drift (follow-up tech-debt)

> Generated by `/mango:codify`, reconciled after ratification. Files diverging from a ratified
> standard. These are **observations for follow-up**, not auto-fixes — mango changed no code.

## Status: effectively clean
The ratified set was chosen to match existing code. Component declaration (`function Name()`),
exports, imports, types, file naming, and logging all match current code — **no drift**.

The main date entry point `solarToLunar` already guards the 1900–2100 range and throws
(`src/core/calendar/solarToLunar.ts:12,22`), satisfying the entry-boundary validation rule there.

## Minor follow-ups (optional, low priority)
1. **English error messages at an entry point** — `solarToLunar` throws English messages
   (`"Year ${year} out of range (1900-2100)"`, `"Date before 1900-01-31 not supported"`), which
   conflicts with the **core-errors rule** (Vietnamese user-facing message). Translate if these can
   surface to the user.
2. **Verify remaining entry points guard date input** — confirm the compare profile builder
   (`src/core/compare/buildProfile.ts` / `compatibility.ts`) and good-day picker
   (`src/core/calendar/goodDayPicker.ts`) validate date range / hour index (0–11) at their boundary,
   per the entry-boundary validation rule. Add a guard where missing.
